# Claude Guidance

## AI Debug Bridge

To co-debug a running Desktop Assistant, see `packages/desktop-assistant/docs/DEBUG_BRIDGE.md`.
Start the app with `DA_DEBUG_BRIDGE=1`; token and port are written to `<agentDir>/debug-bridge.json`.
